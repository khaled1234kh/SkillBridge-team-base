# SkillBridge — Complete Handoff
**Date:** 2026-09-16  
**Status:** Continue from Phase 4C.1. Do **not** start Phase 4D until Live Voice is manually accepted.

## 1. Product
SkillBridge is a GenAI-powered career-readiness platform.

Core loop:
CV / Profile → Target Role → Skill Gap → Diagnostic → Personalized Learning → Learn → Practice → Mini Check → Final Assessment → Verified Skill → Jobs / Career Roadmap

Critical trust invariant:
- CV/self-reported skill ≠ Verified Skill
- Learning ≠ Verified Skill
- Practice ≠ Verified Skill
- Mini Check ≠ Verified Skill
- Chat / Mentor ≠ Verified Skill
- Mock Interview ≠ Verified Skill
- ONLY official Final Assessment pass >=70% may create/update a Verified Skill.

Diagnostic thresholds:
- Mastered >=75
- Developing 40–74
- Weak <40

## 2. Current Local Working Copy
Current user working folder:
`C:\Users\user\Downloads\SkillBridge-main\SkillBridge-main`

App URL:
`http://localhost:8000`

Before any Git action:
```powershell
cd "C:\Users\user\Downloads\SkillBridge-main\SkillBridge-main"
git status
git remote -v
git branch --show-current
```

Do not reset, clean, revert, stash, or delete work before understanding the actual Git state.

Shared GitHub repo:
`https://github.com/khaled1234kh/SkillBridge`

As of this handoff, the shared GitHub repo still showed only the earlier initial upload commit, so the latest local Phase 4A–4C work still needs to be pushed from the correct local Git checkout.

## 3. Environment / Secrets
Never commit:
- `.env`
- `env`
- API keys
- local DBs
- node_modules
- virtual environments

Current local env discovery:
- active folder had `env`
- it did not originally have `.env`
- `backend/app/dotenv_local.py` loads ElevenLabs values from repository-root `.env`
- user copied `env` → `.env`
- shell `ELEVENLABS_API_KEY` was NOT set

After any ElevenLabs key/voice change, fully restart backend.

## 4. Mentor System
Nova — warm explainer — Purple  
Axel — energetic practical coach — Blue  
Sage — calm analytical mentor — Gold  
Vex — precise examiner/challenger — Green

Persona ≠ Mode. Selecting Vex must NOT automatically enter Interview Mode.

## 5. Completed Phases
Phase 1 — Mentor system + onboarding — complete.  
Phase 1.5 — selected-mentor-only chat — complete.  
Phase 2 — conversation memory/trust/context — complete and accepted.  
Phase 3 — persona differentiation/confusion/Vex wit — complete and accepted.  
Phase 4A — Chat UX/conversations/history/New Chat/Clear/current-memory/RTL/actions — human accepted.  
Phase 4B.1 — fullscreen Mentor Live Orb + hands-free loop — human accepted.  
Phase 4B.2 — explicit EN / عربي Live language selector; Auto removed from Live because browser SpeechRecognition was unreliable for Arabic.

No 3D. The 3D experiment was rolled back.

## 6. Phase 4C.1 — Current Phase
Voice production hardening is implemented but **NOT HUMAN ACCEPTED** because real TTS audio is still blocked by ElevenLabs account/environment.

Already implemented:
- four mentor voice configs
- one TTS request per turn
- no duplicate playback
- no recognition during playback
- playback ends before auto-listen resumes
- safe close/end cancellation
- EN + Arabic explicit paths
- Live-only concise spoken-response rule
- developer latency traces
- safe TTS failure handling

Simple Live questions should answer in 1–3 short sentences. Detailed/step-by-step asks may be longer. Normal text chat behavior should remain unchanged.

## 7. Real Latency Findings
Configured GenAI:
- NVIDIA NIM
- active model observed: `nvidia/nemotron-3.5-lightning-30b-a3b`
- thinking disabled for main model
- provider latency can still vary heavily

Optimizations already implemented:
- spoken identity/greeting questions short-circuit deterministically
- no full NIM call for simple identity questions
- `What's your name?` went from ~22–23s previously to tens of milliseconds for tutor text generation
- simple spoken turns use a smaller Live-only token budget
- spoken timeout is bounded
- optional `LIVE_NIM_MODEL` exists but is unset because no verified faster callable model was found

A real `Explain Docker simply` turn measured about ~4.5s on a good draw; NIM can still throttle.

## 8. Current Critical Blocker — ElevenLabs
Debug progression:
1. `/tutor/tts` → ElevenLabs 401
2. after key/env fix → ElevenLabs 404
3. after valid Voice ID → ElevenLabs 402

Current exact response:
```json
{"detail":"ElevenLabs returned 402","request_id":"..."}
```

Interpretation: current blocker is ElevenLabs billing/credits/API-key usage limits, not the frontend voice state machine.

Next account-side checks:
- ElevenLabs Usage / Billing / Subscription
- remaining credits
- API-key Usage Limit
- Text to Speech permission
- restart backend after changes

Do NOT rewrite voice architecture to fix provider-side 402.

## 9. Required ElevenLabs Variables
```env
ELEVENLABS_API_KEY=...
ELEVENLABS_NOVA_VOICE_ID=...
ELEVENLABS_AXEL_VOICE_ID=...
ELEVENLABS_SAGE_VOICE_ID=...
ELEVENLABS_VEX_VOICE_ID=...
```

Vex was missing from an older env file; add it explicitly.
For diagnosis, using one known-good Voice ID for all four mentors is acceptable. After HTTP 200, restore separate voices.

## 10. Immediate Next Steps
1. Resolve ElevenLabs 402 at account/key/credits level.
2. Restart backend.
3. Verify `/api/students/.../tutor/tts` returns HTTP 200.
4. Human-test Nova/Axel/Sage/Vex in EN.
5. Human-test all four in Arabic.
6. Confirm full audio, no cutoff, no duplicate, auto-return to Listening, safe EN↔Arabic switching.
7. Test `Explain Docker simply`.
8. Only then mark Phase 4C.1 HUMAN ACCEPTED.
9. Only after that start Phase 4D.

## 11. Planned Phase 4D — NOT YET
Unified Mentor Live:
- Conversation Mode
- Interview Mode

Same mentor system and Orb.
Do NOT start Phase 4D before 4C.1 acceptance.

## 12. Key Files
Frontend:
- `frontend/src/lib/voiceSession.ts`
- `frontend/src/hooks/useVoiceSession.ts`
- `frontend/src/hooks/useTTSPlayer.ts`
- `frontend/src/components/VoiceMode.tsx`
- `frontend/src/components/CopilotPanel.tsx`
- `frontend/src/lib/tutorI18n.ts`
- `frontend/src/lib/tutorProfiles.ts`
- `frontend/src/index.css`

Backend:
- `backend/app/genai.py`
- `backend/app/tts.py`
- `backend/app/main.py`
- `backend/app/dotenv_local.py`

Tests/checks:
- `frontend/scripts/check-copilot-voice-unit.mjs`
- `frontend/scripts/check-mentor-live-phase4b1.mjs`
- `frontend/scripts/check-tutor-profiles.mjs`
- `backend/tests/test_tutor_spoken.py`
- `backend/tests/test_tts_voice_config.py`
- other tutor/persona/language/conversation suites

Read `AGENTS.md` first.

## 13. Latest Reported Test Baseline
Frontend:
- 10/10 relevant gates green
- voice-unit 226 passed / 0 failed
- `npx tsc --noEmit` clean
- `npm run build` clean

Backend:
- 422 relevant backend tests passed / 0 failed in the latency-return report

Automated green does NOT equal Human Acceptance for Live Voice.

## 14. Git Safety
Before continuing:
- read `AGENTS.md`
- inspect `git status`
- inspect `git diff`
- verify repo root/branch/remote
- do not reset/revert/clean completed work
- do not commit `.env` or `env`

Suggested commit message:
`Phase 4C mentor live voice hardening and latency fixes`

## 15. Current Gate
**Phase 4C.1 — IMPLEMENTED, NOT HUMAN ACCEPTED**

Reason: `/tutor/tts` currently fails with ElevenLabs HTTP 402.

Acceptance requires:
- TTS HTTP 200
- audible reply
- all four mentors
- EN + Arabic
- no cutoff / duplicate / stuck state
- acceptable real response latency

STOP before Phase 4D.
